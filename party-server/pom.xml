<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.2.5</version>
        <relativePath/>
    </parent>

    <groupId>com.hongmai</groupId>
    <artifactId>party-server</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>party-server</name>
    <description>红脉云筑 · 智慧党建小程序服务端</description>

    <modules>
        <module>party-common</module>
        <module>party-audit</module>
        <module>party-auth</module>
        <module>party-org</module>
        <module>party-content</module>
        <module>party-learn</module>
        <module>party-exam</module>
        <module>party-boot</module>
    </modules>

    <properties>
        <java.version>17</java.version>
        <maven.compiler.release>17</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <revision>1.0.0-SNAPSHOT</revision>

        <mybatis-plus.version>3.5.5</mybatis-plus.version>
        <easyexcel.version>3.3.4</easyexcel.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- 内部模块 -->
            <dependency>
                <groupId>com.hongmai</groupId>
                <artifactId>party-common</artifactId>
                <version>${revision}</version>
            </dependency>
            <dependency>
                <groupId>com.hongmai</groupId>
                <artifactId>party-audit</artifactId>
                <version>${revision}</version>
            </dependency>
            <dependency>
                <groupId>com.hongmai</groupId>
                <artifactId>party-auth</artifactId>
                <version>${revision}</version>
            </dependency>
            <dependency>
                <groupId>com.hongmai</groupId>
                <artifactId>party-org</artifactId>
                <version>${revision}</version>
            </dependency>
            <dependency>
                <groupId>com.hongmai</groupId>
                <artifactId>party-content</artifactId>
                <version>${revision}</version>
            </dependency>
            <dependency>
                <groupId>com.hongmai</groupId>
                <artifactId>party-learn</artifactId>
                <version>${revision}</version>
            </dependency>
            <dependency>
                <groupId>com.hongmai</groupId>
                <artifactId>party-exam</artifactId>
                <version>${revision}</version>
            </dependency>

            <!-- 第三方 -->
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-spring-boot3-starter</artifactId>
                <version>${mybatis-plus.version}</version>
            </dependency>
            <dependency>
                <groupId>com.alibaba</groupId>
                <artifactId>easyexcel</artifactId>
                <version>${easyexcel.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <configuration>
                        <excludes>
                            <exclude>
                                <groupId>org.projectlombok</groupId>
                                <artifactId>lombok</artifactId>
                            </exclude>
                        </excludes>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <release>17</release>
                    <parameters>true</parameters>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>${lombok.version}</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
